---
sidebar_position: 1
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { Vimeo } from 'mdx-embed';

# Clash of code

## Division entière du maximum divisé par le minimum

Trouvez le maximum et le minimum d'une liste de nombres positifs et retournez la division entière du maximum divisé par le minimum.

**Entrée**

Ligne 1 : N nombres séparés par un espace.

**Sortie**

Le plancher du maximum divisé par le minimum des nombres donnés.

**Contraintes**

Chaque nombre est différent de 0

**Exemple**

Entrée : 5 1 3 4 8

Sortie : 8

```javascript
/**
* Le code généré automatiquement ci-dessous vise à vous aider à analyser
* l'entrée standard selon l'énoncé du problème.
**/
const numbers = readline().split` `.map(x=>+x);

// Écrivez une réponse en utilisant console.log()
// Pour déboguer : console.error('Messages de débogage...');
console.log(Math.floor(Math.max(...numbers)/Math.min(...numbers)));
```

```javascript title="mon essai non réussi"
/**
 * Auto-generated code below aims at helping you parse
 * the standard input according to the problem statement.
 **/

const numbers = readline();
let min = numbers[0];
let max = numbers[0];
for (i = 0; i > numbers.length; i++) {
    if (numbers[i] < min) {
        min = numbers[i]
    }
    if (numbers[i] > max) {
        max = numbers[i]
    }
}
let answer = max / min;
// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('answer');
```

